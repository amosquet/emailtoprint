# TEMPLATE — Do not use directly.
# Run setup_linux.sh to generate and install the actual user service file dynamically.

[Unit]
Description=Email to Print Service
After=network.target

[Service]
Type=simple
# Replace WorkingDirectory and ExecStart with absolute paths to your repository and virtualenv python
WorkingDirectory=/path/to/emailtoprint
ExecStart=/path/to/emailtoprint/.venv/bin/python main.py
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
